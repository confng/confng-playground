# API Keys and Secrets (marked as sensitive)
# IMPORTANT: Replace these example values with your actual credentials
api.service1.secret-key=example_api_key_replace_with_real
api.service2.api-key=example_api_key_replace_with_real
oauth.client-secret=example_oauth_secret_replace_with_real
jwt.signing-key=example_jwt_key_replace_with_real

# Database Credentials
database.admin.username=admin
database.admin.password=example_password_replace_with_real

# External Service Credentials
service.payment.api-key=example_service_key_replace_with_real
service.analytics.token=example_service_token_replace_with_real
service.monitoring.secret=example_service_secret_replace_with_real

# Encryption Keys
encryption.master-key=example_master_key_replace_with_real
encryption.data-key=example_data_key_replace_with_real
